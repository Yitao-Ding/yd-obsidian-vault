# 2026-07-09 Notion新アカウント移行 — Yitao Film案件管理ダッシュボード再構築

作成: 2026-07-09 / 実行: Claude Code (メイン=Fable 5 + ビルド=Sonnet 5 サブエージェント)

## 状況
YDがNotionアカウントを新規作成 (yitao-ding@ooo-studio.jp = OOO Studio Workspace)。旧ダッシュボード (chat構築 + 6/24実案件投入) は旧アカウントに残るため、引き継ぎ書 `knowledge/filmmaking/yitao-film-notion-handoff.md` を正本として新workspace「DingYitaoさんのスペース」にゼロから再構築した。YDの「今のちょっと見づらい」フィードバックを受け、ホームレイアウトも刷新。

## やったこと
- 3DB (👤クライアント / 🎬案件 / ✅タスク) を新規作成、select配色付き、relation DUAL + ロールアップ (進捗率/タスク数)
- クライアント**8件**・案件**10件**・タスク**84件**を投入 (6/24データ + 進捗反映)
- 進捗反映: 案件5→「JAMBOREE STUDIO BATTLE Lite OP『THE EYE / 開眼』」に改名・📦納品済(7/5)・種別✂️編集のみ・クライアント「JAMBOREE STUDIO BATTLE」新規追加 / 案件8(ナナ)→🎬制作中・セレクトまで完了✅
- ホーム刷新: 使い方callout + 要確認トグル(5点) → 🔥進行中board(納品済除外) → ⏰直近タスクlist(未完・締切順) → 📅納品カレンダー → 👤クライアント → 📦アーカイブ → 🧩新規案件テンプレ。**元DBはis_inline:falseのままリンク表示のみ、閲覧はリンクドビュー経由** (旧版の「3DBベタ置き」を廃止)
- 名称訂正 (YD指摘): 「ダンサレチャリティー」→**ダンスでチャリティー**(ダンちゃり)、「平成立祭」→**平成たち祭**。Notion + 引き継ぎ書 + active_projects.md を修正、Claude Codeメモリにも保存
- 引き継ぎ書のID群を新workspaceに全面更新 + MCPの癖に知見追記

## 新ID (正本=引き継ぎ書 §0)
- ホーム: https://app.notion.com/p/398d8d9d19c281c59d71d986e0a6b63a
- クライアント ds: `8383c057-744e-45a2-8822-8b0a5c7ba7c2` / 案件 ds: `c8b35e29-0b5c-4386-b9ef-9b1c58648a97` (prefix YF) / タスク ds: `ebe7a349-251b-479b-ae3e-9d8c0924476a`

## ✅ うまく行ったこと
- 引き継ぎ書が正本として機能し、旧アカウントのデータに一切アクセスできない状態でも全データ復元できた (「Vaultに書けば残る」の実証)
- ビルドをSonnet 5サブエージェントに委譲 (Fable残量4%対策)。1回目の学び (teal不可・丸数字リテラル・inline方針) を2回目に引き継ぎ、正しいworkspaceでの再構築はエラー0で完走
- 接続先の事前確認 (`notion-fetch id="self"`) をエージェントの開始条件にしたので、2回目は間違いなく新アカウントに構築できた

## ❌ 詰まったこと
- **構築先アカウントの確認不足**: 1回目、接続先が toyo.jp の旧アカウントだと気づかず丸ごと構築してしまった (YDの「まっさらになった」と、旧workspaceにテンプレページしか無い状態が偶然一致して誤認)。fetch self でメールアドレスまで確認し、**YDに「このアカウントで合ってますか」と聞いてから**構築すべきだった
- コネクタの認証切替はClaude側から不可 → YDに claude.ai 設定 / `/mcp` での再接続を依頼する必要がある
- 旧workspaceに作った残骸はMCPから削除不可 → YD手動削除
- クライアント名の誤記 (ダンサレチャリティー/平成立祭) が6/24引き継ぎ書から2回複製された。固有名詞は初出時にYDに表記確認すべき

## 📋 次回同じことをするときのチェックリスト
1. **構築・投入の前に必ず `notion-fetch id="self"` で接続先のメールアドレスとworkspace名をYDに提示して確認を取る**
2. アカウント切替はYD操作 (claude.ai 設定 → コネクタ → Notion 再接続、または `/mcp`)。完了後に再度 fetch self
3. 構築手順・スキーマ・データは引き継ぎ書 §0〜§7 に従う (ID・知見は2026-07-09更新済)
4. DB作成→スキーマ→データ投入→ビュー作成の順。**is_inlineは最初から触らない**
5. select色にtealは不可 / 丸数字はリテラル直書き / ホームtitleに絵文字を入れない (iconのみ) / UNIQUE_ID prefix衝突時は409→フォールバック
6. 固有名詞 (クライアント名等) は初出時にYDへ表記確認

## 関連
- [[yitao-film-notion-handoff]] (正本、ID更新済)
- [[2026-06-24_Yitao_Film案件Notion投入]]
- [[2026-07-08_OOO-Studio_ブランド基盤構築]] (新アカウントのドメイン ooo-studio.jp)
- [[active_projects]]
