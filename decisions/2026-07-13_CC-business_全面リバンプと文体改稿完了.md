---
type: decision
date: 2026-07-13
project: パソコン1台の学校 (CC-business)
tags: [claude-code, workflow, 文体, リバンプ]
---

# CC-business 0→100全面リバンプ + AIっぽさ排除の文体全面改稿 完了

## 経緯

YD指示(2026-07-11)「Opus製の現行ツールはフロント/バック/UI/UXに不満が多い。Fable司令塔で0→100全部見直して改善点を全部修正。実装はOpus以下に」→ 同日追加指示「文章のAIっぽさ(情報量過多・人間が書く文章じゃない感)も試行錯誤して直して」。

体制: Fable=司令塔(計画・検収・味の判断のみ、コンテキスト温存)/ 監査・検収=Fable継承レンズ / 実装=Opus / ゲート=Sonnet。3本のWorkflow(監査23体→実装16体→文体96体)+単発エージェントで計約140体。

## 成果 (全ローカルcommit、push未実施)

1. **全方位監査143件**(静的4レンズ+実機UX/モバイル+ビジュアル/a11y/ペルソナ+性能、客観系P0/P1は反証投票で確定・誤検知0)
2. **8WP実装**: 特商法プレースホルダ露出根治 / 価格単一情報源化(¥15,000ハードコード全廃、先着100名カウンタをpayment_intents.search化) / restore失敗の可視化 / Webhook受け口 / JS無しでLP可視 / コントラスト全AA / コードコピー766箇所 / モバイル可読性 / 進捗・続きから再開 / 404・error境界 / git管理化 ほか。**E2E全通過・リグレッション0**
3. **文体全面改稿**: 診断(段落中央値113字・息継ぎ14%・「正直」71回・全149本同一鋳型)→ VOICE-RULEBOOK(R1〜R15+LP専用L1〜L8、核=語彙でなくリズム)→ パイロット4本→ 38モジュール展開。「正直」71→17、オープナー149本全ユニーク、リンク353/フェンス384/図32は完全一致で無傷
4. git: baseline `5d2b1cb` → Wave0〜3 → voice `69c7f26` → `c361c33`

## ✅ うまく行ったこと

- 「監査→反証検証→WP仕様書化(ファイル所有権を単一化)→Wave実装→ゲート→E2E」のフル機構。ファイル所有権の重複ゼロ設計が並列実装の衝突をほぼ防いだ
- 文体問題を「語彙」でなく「リズム(文末3連続・息継ぎ段落率・鋳型の均質さ)」と定量診断してからルール化 → パイロット4本で較正 → 一括展開、の順番。数量一致の機械ゲート(リンク/フェンス/図/やってみる)で「文体だけ変えて中身は無傷」を証明できた
- セッション上限で96体中15体が死んでも resumeFromRunId で無損失再開(2日跨ぎでも成立)

## ❌ 詰まったこと

- Workflow の args がスクリプトに文字列で届き `pipeline()` が即死 → スクリプト冒頭の防御的 `JSON.parse` で解決(Claude側メモリに記録済み)
- WP5に2体のビルダーが重複着手(リトライ起因の孤児)。クロバー合戦寸前で片方が自主停止→司令塔裁定で収束。教訓: 並列実装は「ファイル所有権」だけでなく「1WP=1体」の保証も要る
- 検収エージェントの一部が「アイドル通知のみ・本文なし」で完了 → 成果物をファイル直読みで検収する運用に切替えて解決

## 📋 次回同じことをするときのチェックリスト

1. 監査は「静的コード+実機ブラウザ+ペルソナ+性能」の4系統を最初から並列で(片方だけだと片肺)
2. WP仕様書に「対象ファイル欄=所有権」を必ず書き、同一ファイルを2WPに与えない。stripe.ts/layout.tsx/globals.cssのような交差点ファイルは各1WPに集約
3. 文体改稿は 診断(頻度grep+段落統計)→声の定義→ルールブック→パイロット→展開 の順。いきなり書き換えない
4. 不変条件(リンク/コード/図/特殊見出し)は編集前に数を採り、ゲートで一致検証
5. Workflowスクリプトは args 防御パース+セッション上限前提で resume 可能に組む
6. 大規模改稿の前に必ず git baseline commit(PII staged チェック付き)

## 関連

- 正本: `~/projects/CC-business/HANDOVER.md` §14 / 監査・仕様書・ルールブック: `CC-business/REVAMP-2026-07-11/`
- YD残作業: 本名→brand.ts / Stripe本番キー+店舗名 / RESEND_FROM / 残席実数表示の判断 / テストURL再デプロイ可否
